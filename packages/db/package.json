{
  "name": "@meloman/db",
  "version": "0.0.1",
  "private": true,
  "exports": {
    ".": "./client.ts",
    "./schema": "./schema/index.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx --env-file=../../.env.local --env-file=../../apps/web/.env.local seed.ts",
    "db:seed:bulk": "tsx --env-file=../../.env.local --env-file=../../apps/web/.env.local seed-bulk.ts",
    "db:clean:test": "tsx --env-file=../../.env.local --env-file=../../apps/web/.env.local clean-test-data.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1041.0",
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.40.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.30.4",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
